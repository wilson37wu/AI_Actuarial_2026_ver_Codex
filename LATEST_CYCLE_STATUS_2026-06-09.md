# Latest Cycle Status - 2026-06-09 (+08)

**Phase 28 Task 3 COMPLETE (PASS, SE gate). Grouped-t-copula margin bootstrap + residual RE-decomposition. Next: Phase 28 Task 4 (tail diagnostics + MR-010/MR-014 refresh decision + OPEN MR-016).**

Ran the grouped-t-copula margin bootstrap (**200 replicates × 20,000 sims**, P26T3/P27T3 pattern) on the FROZEN copula (correlation Sigma, homogeneous df 2.9451, per-block df_NONFIN 37.866 / df_FIN 8.506) and governed relief scalars (sigma/alpha/beta_fit FROZEN, SII Art. 234). Each replicate joint-resamples the realised standalone-loss rows WITH replacement (cross-driver pairing preserved) and re-runs the Phase 28 Task 2 grouped-t component re-aggregation; on **common random numbers** (the SAME latent Gaussian draw on the frozen Sigma) the nested single-df t variant (all df_g = the frozen df, one shared mixing variate) is also evaluated, so the per-replicate (grouped-t − single-t) difference isolates the per-block-df heterogeneity effect. The per-block radial mixing IS the lever, so the CRN shares the Gaussian copula latent only — exactly mirroring the Phase 27 Task 3 skew-t pattern (which shared Z and W and varied only gamma).

**Result.** Grouped-t component SCR mean **35,372.5**, 95% percentile CI **[33,034.4, 38,008.5]**, SE **3.58%** of mean (≤ 5% gate PASS). HEADLINE: the nested path-wise truth **46,638.9 lies OUTSIDE the grouped-t 95% CI** → the residual gap is RE-decomposed. The per-block df fitted leakage-free to the standalone within-block upper co-exceedances DILUTE cross-block co-movement, so the copula-form residual **WIDENS from the skew-t-reconfirmed 6,114.9 to 10,491.5** at the canonical 200k point (+4,376.6 / **+71.57%**; vs frozen-t 6,120.2 +4,371.3 / +71.42%); the grouped-t is **RE-CONFIRMED as NOT closing** the upward nested residual. DIRECTIONAL diagnostic (DISCLOSED, NOT gated — the grouped-t is genuinely two-sided): grouped-t − single-df t CRN mean **−4,222.6 (DOWN; 100% of replicates negative)**. Attribution: this is the **second negative super-set result** after the Phase 27 skew-t (gamma_hat~0) — a copula on the STANDALONE margins, whether asymmetric or block-heterogeneous, cannot close the UPWARD nested residual; it lives in nested inner-path joint dynamics → **vine / pair-copula (Aas et al. 2009) escalation flagged for Phase 29**.

**Gates (pre-registered, design note s5):** C1 headline (nested OUTSIDE CI → gap RE-decomposed + change vs the skew-t-reconfirmed 6,114.9 quantified) PASS; C2 directional DISCLOSED not gated PASS; C3 SE ≤ 5% PASS (3.58%); C4 archive cross-check bit-identical (frozen-t 39,975.654628 + grouped-t-at-df_hat 35,604.398946) PASS; C5 copula/scalars FROZEN PASS; C6 chunk-independent + idempotent (digest 71d5c6eee80d, re-run bit-identical) PASS; C7 governance PASS.

**Verification:** pytest **8/0** (P28T3); regression P28T1-T2 + P28T3 **35/0**, P27T1+T4 **25/0**, P27T2 **13/0**, P27T3 **10/0** (**83 passed, 0 failed**); compileall clean; report + governance JSON validated. Governance: ChangeRecord `32bae3c32f0040a08a9696c23e02a062` (methodology_change) OWNER_REVIEW; audit 88→89; change records 60→61; verify_all True; idempotent (re-run added:false). GOVERNANCE_STORE.json + MODEL_DEV_STATE.json backed up + parse-verified pre AND post (`/var/tmp/p28t3_stage/GOV_BACKUP_pre_p28t3.json`, `STATE_BACKUP_pre_p28t3.json`).

**New files / reports:**
- par_model_v2/projection/grouped_t_copula_bootstrap.py
- scripts/build_phase28_task3_grouped_t_bootstrap.py
- tests/test_phase28_task3_grouped_t_bootstrap.py
- docs/validation/PHASE28_TASK3_GROUPED_T_BOOTSTRAP_REPORT.{json,md}
- docs/GROUPED_T_BOOTSTRAP_CARD.md

**Next executable task: Phase 28 Task 4** — grouped-t tail diagnostics (within/cross-block upper/lower tail-dependence asymmetry vs single-df t on CRN) + MR-010/MR-014 refresh decision (the headline component SCR move is large vs the single-df t but the grouped-t direction is DOWN; the >1% trigger and MR refresh are assessed against the governed frozen-t component basis) + **OPEN MR-016** for the heterogeneous-tail / cross-block-dilution copula-form change. Then Task 5 (offline-UI propagation, contract 1.9.0 → 1.10.0 ADDITIVE: grouped-t vs single-df t vs nested SCR, bootstrap CI, residual re-decomposition + widening, df_NONFIN/df_FIN, MR-016; UI consumes ONLY model-output JSON, zero-install). On Phase 28 completion → **Phase 29 vine / pair-copula (Aas et al. 2009)**.

**Standing blockers (human action):** git ghost locks (GITHUB_PUSH_BLOCKER.md) — sandbox commits/pushes blocked; this cycle's artefacts are on the mount but NOT pushed; production sign-off withheld pending credentialled data + independent APS X2 review (by design, educational). The fully-offline zero-install model-output-only interactive UI requirement remains SATISFIED. Sandbox note: scipy/pytest are pip-installed fresh each cycle (no persisted /var/tmp/pylibs this run); the staged P23T2/P23T4/P26T2/P28T2 npz inputs survived on /var/tmp and were reused.

---
