# Post-Phase-IGUI Outer-Loop Efficiency Design Card (MR-VR-2)

**MR-VR-2 - RQMC + control-variates variance reduction for the OUTER capital (SCR) loop.** Pure governance pre-registration; NO parameter change; implementation DEFERRED. EFFICIENCY only.

## What it would do (next cycle, gated)

Run a DISCLOSED efficiency study on the OUTER capital / SCR loop: compare crude i.i.d. Monte Carlo against scrambled-Sobol randomised-QMC over the outer scenario grid, a control variate built from the cheap closed-form / proxy SCR, and an optional proportional tail stratification - all targeting the governed 99.5% SCR over the SAME governed model. Report unbiasedness (control beta fit out-of-sample; RQMC mean over >= 200 scramble seeds within 0.5% of crude), work-normalised variance-reduction ratios with CIs at the 99.5% target, the control-target correlation rho with realised reduction 1/(1-rho^2), and the outer scenario count n* for a target SCR SE. The governed production estimator and headline SCR (39,975.7) stay BIT-IDENTICAL - this is measurement, not adoption.

## Why now

MR-CAL-1 calibration diagnostics (Task 2) and MR-VR-1 inner-path variance reduction (Task 4/5) are both COMPLETE. MR-VR-1 helped the MEAN-TVOG target a great deal but recorded antithetic as INEFFECTIVE (1.31x) at the 99.5% quantile - which is exactly the OUTER loop's target. The remaining admissible numerical gain is on the OUTER SCR estimator, where RQMC and a control variate are the bias-free levers that suit a tail target.

## Sequencing

1. **MR-CAL-1** credentialled-data calibration diagnostics (COMPLETE, Task 2).
2. **MR-VR-1** inner-path antithetic/CRN/RQMC variance reduction for TVOG (COMPLETE, Task 4/5).
3. **MR-VR-2** RQMC + control-variates for the OUTER capital/SCR loop (selected now).
4. **MR-LONGEV-1** longevity 5th driver (deferred; needs owner sign-off).

*Generated by scripts/build_postigui_task6_design_note.py.*
