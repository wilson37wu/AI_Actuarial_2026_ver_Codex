# Calibration Backtest Report 2026

**Generated:** 2026-05-22T22:37:36Z  
**Status:** Development report scaffold implemented; runtime population blocked in this shell  
**Deliverable owner:** `par_model_v2/calibration/backtest_reporting.py::BacktestReport`

---

## 1. Scope

This annual report file is the Phase 4 backtesting deliverable referenced in `docs/PARAMETER_CALIBRATION_METHODOLOGY.md §9.4`. The report generator is now implemented in code and produces:

- 10th-90th coverage diagnostics for annual realised rate and equity outcomes
- VaR95 / VaR99 breach rates with Kupiec proportion-of-failures p-values
- Expected Shortfall tail metrics (ES95 / ES99)
- Tail breach severity metrics (mean and maximum realised excess above VaR)
- Worst-observation summary for governance review
- Recalibration recommendation based on SOA / IA / ERM thresholds

## 2. Current Runtime Blocker

The current shell does not expose a Python environment with `numpy`, `pandas`, `scipy`, and `pytest` on PATH. A bundled interpreter was found at `C:\Program Files\PostgreSQL\18\pgAdmin 4\python\python.exe`, but it lacks the scientific stack required to execute the synthetic backtest and emit a populated annual report.

As a result, this file documents the reporting surface and the code path, but the first fully populated markdown instance must be regenerated by calling:

```python
from par_model_v2.calibration import (
    BacktestDataset,
    BacktestEngine,
    BacktestReport,
)
```

and then writing `BacktestReport(...).write("docs")` from an environment with the required dependencies installed.

## 3. Validation Completed This Cycle

- `par_model_v2/calibration/backtesting.py` syntax check passed via `py_compile`
- `par_model_v2/calibration/backtest_reporting.py` syntax check passed via `py_compile`
- `tests/test_backtesting.py` syntax check passed via `py_compile`

## 4. Report Contents Once Runtime Is Available

The generated markdown report will include these sections:

1. Executive summary
2. Tail loss analysis
3. Worst observation
4. Governance interpretation
5. Recommendation
6. Machine summary

## 5. Next Action

Run the new reporting workflow in the project Python environment, regenerate this file with live synthetic backtest outputs, and then proceed to Phase 4 sensitivity analysis.
