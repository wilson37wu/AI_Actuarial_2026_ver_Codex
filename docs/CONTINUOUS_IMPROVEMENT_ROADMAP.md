# Continuous Industry-Grade Improvement Roadmap

**Document ID:** `CONT-IMPROVEMENT-ROADMAP`
**Created:** 2026-07-03
**Owner:** Model Owner (KCW) — executed by Claude Cowork scheduled task `actuarial-model-daily-improvement`
**Cadence:** Daily, 14:00 local (06:00 UTC — the Claude slot per `AGENT_COORDINATION.md`)
**Governing protocol:** `AGENT_COORDINATION.md` (lock, one task per cycle, throwaway-clone git, push to `main`)

---

## 1. Objective

Advance the PAR Endowment Stochastic ALM & TVOG model from "structurally complete, NOT production ready" toward industry-grade production quality, one governed increment per cycle. Every cycle must leave `main` with: passing tests, updated documentation, a governance note, and no regression to governed headline figures without explicit sign-off.

## 2. Cycle Contract (every scheduled run)

1. Integrate: fresh throwaway clone of `origin/main` (never git-write against the mounted `.git`).
2. Lock: `agent_lock.py preflight` → yield if held; else `acquire`.
3. Select ONE item: the highest-priority `OPEN` item in §4 backlog (or the `in_progress` item in `.claude-dev/MODEL_DEV_STATE.json` if the auto-dev state machine has one).
4. Implement + test: code, unit tests, and self-checks must be GREEN before commit.
5. Document: update the relevant `docs/` card, this roadmap's §5 log, and a uniquely named `docs/cycle_status/LATEST_CYCLE_STATUS_<date>_<id>.md`.
6. Push to `main` (fetch-rebase, retry ≤3×), release lock.
7. If blocked (owner sign-off required, data unavailable), mark item `BLOCKED(reason)` in §4, pick the next item, and surface the blocker to the Model Owner in the run summary.

## 3. Industry-Grade Quality Bar

Each improvement is judged against these standards (traceable in `docs/SOA_ASSUMPTIONS_DOCUMENT.md`, `docs/IA_VALIDATION_REQUIREMENTS.md`, `docs/GOVERNANCE_FRAMEWORK.md`):

- SOA ASOP 56 (modeling), ASOP 25 (credibility), ASOP 7 (cashflow testing)
- IA (HK) TAS M — model governance, independent review, change control
- CBIRC C-ROSS — scenario count (≥2,000), 3.0% discount cap, stress suite
- Reproducibility: seed policy, parameter snapshots, SHA-256 audit trail on every run

## 4. Prioritised Backlog

### 4.0 OWNER-DIRECTED GUI Run-Console Track (directive 2026-07-03, KCW)

Owner directive (2026-07-03, interactive session): build a comprehensive GUI so users configure and trigger calculation runs and view outputs entirely in the GUI — eliminate any need to open .py files. Owner-selected architecture: **local Python web server** (Flask/FastAPI, launched via `python -m par_model_v2.gui` + a Windows launcher script); browser GUI posts run requests, shows progress, renders results in-page.

**This track is COMPLETE (GUI-1..GUI-4 all DONE 2026-07-03); priority reverts to the general backlog (§4.1).** One phase per cycle. It does NOT constitute sign-off for the separately owner-gated Phase 38 Task 3 (`ui_app.html` native-tab cutover / sha256 re-baseline), which stays gated.

| # | Item | Definition of done | Status |
|---|------|--------------------|--------|
| GUI-1 | Run-console server skeleton + core stochastic run: background job execution with progress endpoint, results rendered in GUI. Discovery: the Phase IGUI stack already provided input pages, validation gate, launchers, and a synchronous run; GUI-1 delivered the missing async layer (JobManager, /execute-async, /jobs/<id>, /jobs, polling run page) on the existing stdlib-only server (kept over Flask/FastAPI: zero new deps, same architecture class - local web server) | User can launch, configure, run, watch progress, and read results with zero .py editing | DONE(2026-07-03, see §5) |
| GUI-2 | Sensitivities & stress in GUI: /stress console, 7-item input-level catalogue (re-validated + re-gated per stress, isolated output dirs), base-vs-stress delta table, deterministic Phase 9 asset-stress panel | Stress results in GUI with base-vs-stress deltas | DONE(2026-07-03, see §5) |
| GUI-3 | Calibration runs in GUI: trigger HW1F/GBM calibration via live market-data pipeline (roadmap #1), fit diagnostics (SSE, convergence, parameter card) displayed; UNSIGNED flag surfaced | Calibration runnable + diagnosable from GUI | DONE(2026-07-03, see §5) |
| GUI-4 | Run history & compare: persisted run registry (id, timestamp, inputs hash, seed, headline outputs), list/open past runs, side-by-side diff of two runs | Reproducible run registry browsable in GUI | DONE(2026-07-03, see §5) |
| GUI-5 | One-click Save & RUN on the Run Controls page (owner request 2026-07-03, interactive): save → optional governed-default auto-fill of missing sections → Task-6 gate → async engine run with in-page progress + results links | User kicks off the calculation from the first screen with one button | DONE(2026-07-03, see §5) |

Standing constraints: runs executed through the GUI must keep the existing governance trail (seed policy, parameter snapshots, SHA-256 audit) — the GUI is a front-end to the governed engine, never a bypass. Governed headline figures remain untouched except where an item explicitly covers them.

### 4.0c OWNER-DIRECTED Cash-Flow Projection Set Track (directive 2026-07-03, KCW)

Owner directive (2026-07-03, interactive): a NEW OUTPUT SET with liability cash flows by product class x type (premium, expense, benefits split guaranteed/non-guaranteed across death/surrender/maturity + cash dividend; >=6 buckets for CD products) and asset cash flows AND balances by asset class, monthly + yearly to 100 years. Owner-selected basis: deterministic central. Owner-selected delivery: JSON + CSV + GUI tab.

| # | Item | Definition of done | Status |
|---|------|--------------------|--------|
| CF-1 | Projection engine + artifacts: `cashflow_projection_set.py` (3 product classes, 9 liability buckets, asset CF+balances, monthly 1..1200 + yearly 1..100, JSON+6 CSVs, inputs digest, UNSIGNED declaration-scale note); conventions regression-tested against the legacy per-product engine | Engine + files + tests GREEN | DONE(2026-07-03, see §5) |
| CF-2 | Run integration: /save-run & execute_run attach the CF set to run_output + run registry; stale-set guard (inputs digest match) | CF set produced with every GUI run | DONE(2026-07-08, see §5) |
| CF-3 | GUI tab: yearly view + monthly drill-down per product class / asset class, CSV downloads, UNSIGNED banner | CF set browsable in GUI | DONE(2026-07-03, see §5) |

### 4.0e OWNER-DIRECTED GUI Stepwise-Detail Track (directive 2026-07-07, KCW)

Owner directive (2026-07-07, interactive): enrich the GUI to output and display MORE DETAILED STEPWISE CALCULATION in general - economic scenario paths, asset return paths, asset cash flow by asset class, liability cash flow by guaranteed / non-guaranteed, etc. Delivery pattern per item: engine artifact (JSON+CSV, digest-cached, UNSIGNED note) + self-contained GUI page/section (inline-SVG, zero external refs). Diagnostic overlays only - governed headline figures untouched.

| # | Item | Definition of done | Status |
|---|------|--------------------|--------|
| GD-1 | Scenario-path detail set + /paths GUI page: real-world (P) HW1F short-rate + correlated GBM equity paths on the SAVED run seed; per-asset-class monthly-return & cumulative-index percentile fans (p5/25/50/75/95) with CF-1 class mechanics; sample-path overlays; JSON+6 CSVs digest-cached; CF-3 liability chart gains guaranteed vs non-guaranteed benefit series | Paths browsable in GUI, artifacts on disk, tests GREEN | DONE(2026-07-07, see §5) |
| GD-2 | Stepwise liability drill-down: per-model-point / per-product-class bucket-level cash-flow inspector (pick a policy/class, see month-by-month premium/expense/benefit build-up incl. gtd/non-gtd split) | Policy-level stepwise table in GUI | DONE(2026-07-07, see §5) |
| GD-3 | Stepwise run-result decomposition: surface per-driver standalone SCR paths / TVOG build-up steps from the run artifacts in the results page (calculation waterfall view) | Waterfall of headline build-up in GUI | DONE(2026-07-07, see §5) |
| GD-4 | Scenario-path detail bound to EXECUTED runs: persist the GD-1 set with each run (run registry attachment, like CF-2) so paths shown match the run actually executed, not just current inputs | Per-run path detail in history/compare | DONE(2026-07-08, see §5) |

### 4.0d OWNER-DIRECTED Portfolio Construction Track (directive 2026-07-03, KCW)

Owner directive: flexible input construction - asset classes by type with mix/SAA, and liability product templates (e.g. short vs long term par) composable into the portfolio. **Track COMPLETE (PC-1, PC-2 both DONE 2026-07-08); priority reverts to the general backlog (§4.1).**

| # | Item | Definition of done | Status |
|---|------|--------------------|--------|
| PC-1 | Construction layer + GUI: `portfolio_construction.py` (asset strategy with SAA weights deriving the balance sheet + fund mechanics; product catalogue parameterised over the three mechanic families; composer validation incl. term ranges and the illiquid-share engine rule), CF-engine integration (per-product output classes, catalogue rates, SAA mechanics), `/portfolio` GUI page, loader-revalidated save | Construct + save + gate + run + cashflows end-to-end from composed inputs | DONE(2026-07-03, see §5) |
| PC-2 | Extend mechanic families (whole-life par, term assurance, annuity) + per-product expense/decrement overrides | New families runnable end-to-end | DONE(2026-07-08, see §5) |

### 4.0f OWNER-DIRECTED User Economic-Scenario File Track (directive 2026-07-08, KCW)

Owner directive (2026-07-08, interactive): allow USER-INPUT economic scenario files alongside the built-in HW1F+GBM ESG. Format designed and owner-confirmed same session (decimal annual spot zeros at 12 tenors 3M..30Y, curve at end of projection year; equity annual total return; bond returns engine-derived from the curve; manifest declares basis risk_neutral|real_world + csv_sha256). Spec: `docs/ECONOMIC_SCENARIO_FILE_FORMAT.md` (schema `esg-user-scenarios-1.0`) + templates in `docs/templates/`. User files are UNSIGNED scenario inputs; every run records the file digest. This track outranks the general backlog (§4.1).

| # | Item | Definition of done | Status |
|---|------|--------------------|--------|
| ES-1 | Format spec + validating loader: spec/template (committed this cycle) + `user_scenarios.py` (header/grid/bounds/manifest/sha256 fail-loud validation, summary echo card, UNSIGNED banner) + tests | File validated + loadable, tests GREEN | DONE(2026-07-08, see §5) |
| ES-2 | GUI upload: /scenarios page - upload, validate with row/col errors surfaced, percentile fan-chart preview, persist with digest, gate integration | Upload + preview from GUI, zero .py editing | DONE(2026-07-08, see §5) |
| ES-3 | Engine integration: `scenario_source: model|user_file` run-config selector; measure guard (risk_neutral for valuation, real_world for P-diagnostics; mismatch = ERROR w/ deviation record); file digest + manifest into the run governance trail; monthly interpolation mapping documented | Runs executable end-to-end on user scenarios | DONE(2026-07-09, see §5) |

### 4.1 General backlog


Priority = (regulatory gate) > (model-risk register CRITICAL) > (accuracy) > (capability expansion). Status values: `OPEN`, `IN_PROGRESS`, `BLOCKED(reason)`, `DONE(date, commit)`.

| # | Item | Maps to | Definition of done | Status |
|---|------|---------|--------------------|--------|
| 1 | Live market-data pipeline: CNY yield curve + CSI 300 loaders with schema validation and cached snapshots | MR-006 | Loader module + fixtures + tests; `docs/ESG_CALIBRATION_DATA_INTERFACES.md` updated | DONE(2026-07-03, see §5) |
| 2 | Execute HW1F swaption calibration on live/proxy quote set; parameter card with fit diagnostics | MR-001, MR-008 | `calibrate()` runs end-to-end; SSE/convergence report; params flagged UNSIGNED pending owner approval | DONE(2026-07-08, see §5) |
| 3 | CBIRC 3.0% discount-cap remediation: config default ≤3.0% + deviation record for any override | MR-002 | Validator ERROR (not WARNING) above cap without an approved ChangeRecord | DONE(2026-07-09, see §5) |
| 4 | Dynamic lapse: rate-differential lapse response function with bounded elasticity + sensitivity tests | MR-003 | Model + tests + `PHASE13_DYNAMIC_LAPSE_REPORT.md` refresh; TVOG delta quantified | DONE(2026-07-09, see §5) |
| 5 | Scenario adequacy at 2,000+ scenarios: convergence study 500→1,000→2,000→5,000 with CI bands | C-ROSS gap #6 | Convergence report; runtime benchmark; recommendation memo | DONE(2026-07-09, see §5) |
| 6 | Backtest on real history: populate BacktestEngine with live CNY curve / CSI 300 series (item 1 dependency) | Limitation #5 | Kupiec POF + coverage tests on ≥10y history; recalibration triggers evaluated | DONE(2026-07-09, see §5) |
| 7 | G2++ two-factor rate model promotion: wire existing G2PP cards/design into the production ESG path with curve-twist validation | MR-004 | G2++ selectable in ESG config; martingale + swaption fit evidence; HW1F kept as fallback | OPEN |
| 8 | Stochastic bonus declaration: make RB/TB declaration fully pathwise (extend PATHWISE_DECLARATION work into TVOG main path) | Limitation #4 | Pathwise TVOG vs current TVOG bridge quantified and documented | OPEN |
| 9 | Independent-review readiness pack: assemble APS X2 / TAS M §3.6.5 evidence bundle (validation reports, limitations, sign-off states) | Gate 3 | Single `docs/INDEPENDENT_REVIEW_PACK.md` index; all links resolve | OPEN |
| 10 | Performance: profile 100k-policy batch, close top hotspot (chunking / vectorisation), publish benchmark | Expansion plan §2.6 | ≥20% runtime cut on benchmark or documented finding that none is available | OPEN |
| 11 | Mortality improvement + credibility blending (ASOP 25) for qx tables | Accuracy | Blended table generator + tests + assumptions register entry | OPEN |
| 12 | Model health check expansion: add VR-H11 (calibration drift) and VR-H12 (scenario file schema hash) | Governance | Checks wired into `model_health.py` + scheduled cycle | OPEN |

Items requiring human sign-off (owner approval, regulator deviation): implement to the point of sign-off, then `BLOCKED(owner-signoff)` — never self-approve.

## 5. Cycle Log

| Date | Item | Outcome | Commit |
|------|------|---------|--------|
| 2026-07-09 | #6 Backtest on real history (Limitation #5, roadmap §4.1) — COMPLETES the highest-priority OPEN general-backlog item and CLEARS §4.1 items #1–#6. NEW `par_model_v2/calibration/live_history_backtest.py` wires roadmap item #1's live market-data pipeline into the governed Phase-4/Phase-13 `BacktestEngine`: `CNYBacktestHistoryLoader` SUBCLASSES item #1's `_BaseMarketDataLoader`, so the annual CNY 1Y-rate + CSI 300 return series are resolved through the SAME three provenance tiers (live_fetch→cached_snapshot→file_fixture; second load promotes to the cached tier, regression-tested), `SnapshotCache` SHA-256 sealing, and `DataLineageRecord` (IA TAS M §3.6) that item #1 shipped — no credentialled feed ships so the offline default resolves file_fixture UNSIGNED. `PipelineBacktestHistorySource` adapts that into the existing Phase-13 `BacktestHistorySource` contract, so the UNCHANGED governed path (`LiveBacktestDataLoader`→`calibrate_from_history` in-sample-only→`BacktestEngine`) computes Kupiec POF + rate/equity band coverage on the ≥10-year window (12 annual obs 2014–2025, 7y in-sample / 5y OOS holdout; G-09 PASS on the proxy fixture: rate cov 75%, equity cov 100%, Kupiec VaR95 p=0.267, VaR99 breach 0%). NEW structured RECALIBRATION-TRIGGER evaluator `evaluate_recalibration_triggers` (the item DoD's second clause, beyond Phase 13's single boolean): 8 per-signal triggers (rate/equity coverage, Kupiec VaR95/99 POF, VaR99/VaR95 breach rates, Q-measure martingale, in-sample-vs-OOS coverage drift), each with severity + recommended action, folded to an overall recommendation (CRITICAL→RECALIBRATION_REQUIRED, HIGH→SCHEDULE_RECALIBRATION, MEDIUM→ENHANCED_MONITORING, else NO_ACTION_MONITOR); current run → SCHEDULE_RECALIBRATION (one HIGH `oos_coverage_drift` from the small 5y OOS window; all VaR/coverage/martingale clear). HARDENING: `backtesting._kupiec_pof_pvalue` is now scipy-free — NEW `_chi2_sf_df1` uses scipy when present else the EXACT df=1 closed form `chi2.sf(x,1)=erfc(sqrt(x/2))` (math.erfc, stdlib), numerically identical, so the Kupiec test runs in the offline sandbox. Evidence: `docs/validation/LIVE_HISTORY_BACKTEST.json` (schema live-history-backtest-1.0, stable inputs_digest 56aaa654…, UNSIGNED banner) + builder `scripts/build_live_history_backtest.py` + card `docs/LIVE_HISTORY_BACKTEST_CARD.md`; MODEL_STABILITY_AND_LIMITATIONS.md §3.5 gains an item-#6 partial-mitigation pointer. Purely additive DIAGNOSTIC — governed portfolio TVOG/aggregation headline UNTOUCHED (re-baselining onto a live calibration stays owner-gated; module imports no run_model, regression-asserted). Tests: NEW tests/test_live_history_backtest.py 19/19 GREEN (unittest, numpy/pandas, scipy-free); regression GREEN via a minimal pytest shim — test_backtesting 18/18, test_phase13_backtest 24/24, test_live_market_data_pipeline 12/12 (54/54); pip/scipy/pytest unavailable in the network-restricted sandbox. NEXT OPEN: §4.1 #7 G2++ two-factor rate-model promotion | DONE | (this cycle's AUTO commit) |
| 2026-07-09 | #5 Scenario adequacy at 2,000+ scenarios: convergence study 500->1,000->2,000->5,000 with CI bands (C-ROSS gap #6, roadmap §4.1) - COMPLETES the highest-priority OPEN general-backlog item. NEW `par_model_v2/analysis/scenario_adequacy.py` (numpy-only) runs the GOVERNED `TVOGEngine` unchanged across the mandated ladder and reports, per count, the TVOG point estimate with **95% CI bands**, a runtime benchmark, and a scenario-count **recommendation memo**. KEY FINDING: the governed ESG draws **antithetic** shocks (esg_process `_antithetic_normals`), so the naive iid error `sigma_PV/sqrt(N)` OVERSTATES the true Monte-Carlo error ~10x; the module therefore computes BOTH a naive-iid SE and an **empirical antithetic-aware SE** (std of TVOG across independent governed seeds) and bases the CI/sizing on the empirical model (iid retained as a conservative reference; realised variance-reduction factor reported). Full-ladder result (rep=8, seed 42, det 3.0% CBIRC cap): TVOG 8,514->8,528->8,490->8,461 (each rung inside the previous 95% CI; stable from N=500); effective SE 47.6/42.9/27.7/17.3 vs iid 571.9/405.0/280.8/174.1 (VRF ~10.0x at N=5,000); MC error scaling exponent -0.460 vs theoretical -0.500. RECOMMENDATION: at the CBIRC C-ROSS >=2,000 floor the 95% CI half-width is already ~0.63% of |TVOG| (target <=2%), so the **regulatory floor - not precision - binds; recommended production count = 2,000** (naive iid would over-provision to ~20,341). Artifacts docs/SCENARIO_ADEQUACY_CONVERGENCE_STUDY.{md,json} (schema scenario-adequacy-convergence-1.0, sha256 inputs digest, UNSIGNED banner) + builder scripts/build_scenario_adequacy_study.py; MODEL_STABILITY_AND_LIMITATIONS.md §2.1 gains a pointer. Purely additive DIAGNOSTIC - governed portfolio TVOG/aggregation headline UNTOUCHED (re-baselining onto a revised scenario count stays owner-gated). Tests: NEW tests/test_scenario_adequacy.py 24/24 GREEN (unittest, numpy-only); regression tests/test_sensitivity.py 45/45 GREEN via a minimal pytest shim (scipy/pytest unavailable in the network-restricted sandbox). NEXT OPEN: §4.1 #6 backtest on real history | DONE | (this cycle's AUTO commit) |
| 2026-07-09 | #4 Dynamic lapse — bounded-elasticity response + TVOG delta (MR-003, roadmap §4.1) — COMPLETES the highest-priority OPEN general-backlog item. Phase 13 already shipped the dynamic-lapse FORM (arctan efficiency × logistic mass lapse); this cycle adds the two pieces the item's DoD names and Phase 13 lacked: (a) BOUNDED ELASTICITY — new analytic `marginal_response` (d lapse/d spread), `efficiency_multiplier_slope`, `mass_lapse_slope`, `semi_elasticity`, and a closed-form Lipschitz bound `marginal_response_bound = base·beta·2/(pi·kappa) + shock_max/(4·width)` on `DynamicLapseAssumption` (purely additive; Phase 13 form/params/gates untouched) — a dense finite-difference sweep confirms observed max slope 5.168 ≤ analytic bound 6.601, and the analytic derivative matches central differences to <1e-9; (b) TVOG DELTA QUANTIFIED — new `par_model_v2/projection/dynamic_lapse_tvog.py` computes a representative-policy TVOG proxy `E[PV_netliab(R)] − PV(central)` over `R~N(credited,sigma^2)` by exact 5-node Gauss-Hermite quadrature (SciPy-free): static lapse is rate-invariant so TVOG_static=0 exactly, TVOG_dynamic=−753.60 at sigma=100bps (−1.835% of the central reserve), with a documented vol profile (0.25%→2.0%) showing the mass-lapse turnover sign-flip beyond ~150bps. Discount held at the governed 3.0% cap. Evidence: `docs/DYNAMIC_LAPSE_ELASTICITY_TVOG_DELTA.json` + builder `scripts/build_dynamic_lapse_tvog_report.py`; `docs/PHASE13_DYNAMIC_LAPSE_REPORT.md` gains §6 (bounded elasticity) + §7 (TVOG delta). Purely additive diagnostic — governed portfolio TVOG headline UNTOUCHED (re-baselining onto a dynamic-lapse basis stays owner-gated). Tests: NEW tests/test_dynamic_lapse_elasticity_tvog.py 21/21 GREEN (unittest, numpy-only); existing tests/test_dynamic_lapse.py 27/27 GREEN via a minimal pytest shim (scipy/pytest unavailable in the network-restricted sandbox — calibration used the deterministic coordinate-descent fallback, which meets the existing tolerances). NEXT OPEN: §4.1 #5 scenario adequacy convergence study | DONE | (this cycle's AUTO commit) |
| 2026-07-09 | #3 CBIRC 3.0% discount-cap remediation (MR-002, roadmap §4.1) - COMPLETES the highest-priority regulatory-gate item: the CBIRC 3.0% reserving-discount cap is now a HARD VALIDATION ERROR (was a soft WARNING) unless an APPROVED deviation ChangeRecord authorises the specific override, in which case it degrades to a governed WARNING. DiscountRateValidator D3-R01 (scalar) + D3-R03 (term structure) are ERROR-by-default; NEW duck-typed helper `discount_rate_deviation_approved(records, rate, cap)` recognises a deviation only when an APPROVED `assumption_change` ChangeRecord carries after_snapshot['discount_rate_annual'] STRICTLY above the cap AND equal (1e-9) to the validated rate (each override needs its own sign-off; the MR-001 at-cap record never authorises above-cap); `validate()` gains approved_deviation/governance params, `validate_all()` gains discount_rate_approved_deviation + auto-consults governance_store; helper exported from the package; doc card docs/CBIRC_DISCOUNT_CAP_REMEDIATION.md. Purely additive guard - NO governed headline change (no run path uses >3.0%; MR-001 G-01 gate still PASS at the 3.0% default; model_health 0.025 path clean). Tests: NEW tests/test_mr002_discount_cap_remediation.py 22/22 GREEN (unittest); 2 existing pytest tests updated to the remediated contract (test_data_validator legacy-rate, test_phase13_mr001 legacy-rate) + 32/32 fixture-free affected pytest-file tests GREEN via a minimal pytest shim; pytest/scipy unavailable in the network-restricted sandbox so the broad pytest suite was not collected (numpy/pandas subset + targeted shim used). NOTE reclaimed a STALE same-owner lock (a prior claude cycle acquired #3 at 2026-07-08T23:20Z then crashed pre-work - double-acquire, no work commit; expired ~9h). NEXT OPEN: §4.1 #4 dynamic lapse | DONE | (this cycle's AUTO commit) |
| 2026-07-09 | ES-3 (owner directive 2026-07-08: user economic-scenario files - COMPLETES track 4.0f): ENGINE INTEGRATION of user scenario files into the governed run pipeline. NEW `par_model_v2/stochastic/scenario_source.py` (numpy) - the annual->monthly PIECEWISE-ANNUAL interpolation mapping (schema es3-scenario-source-1.0): each year-end spot-zero curve held across its 12 months, monthly short-rate proxy = the 3M spot, monthly equity = geometric split (1+annual)^(1/12)-1 recompounding to the annual EQ_RETURN EXACTLY (<1e-9, regression-tested incl. the spec's -0.99 equity bound staying finite); returns short_rate/equity_return/rate_cube/year_index + a JSON-safe mapping summary card. NEW `par_model_v2/viewer/igui_scenario_source.py` (stdlib-only at import, numpy/loader/engine lazy - GUI-layer contract, regression-tested) - the `scenario_source: model|user_file` selector + `run_intent: valuation|p_diagnostic` reader (fail-loud on bad values), the MEASURE GUARD (valuation->risk_neutral, p_diagnostic->real_world; a mismatch / missing / unknown-basis file is an ERROR carrying a structured SCENARIO_MEASURE_DEVIATION record - no silent mis-measurement), a `build_scenario_source_provenance` block (schema es3-scenario-source-prov-1.0: selector, guard decision, file csv_sha256 + manifest subset, monthly-mapping summary, UNSIGNED banner, deviation record if any), and `attach_scenario_source_for_run` (digest-keyed persistence under run_output/scenario_source_runs/<digest12>/ like CF-2/GD-4, persisted-CSV digest RE-VERIFIED on attach -> STALE on tamper/missing, cache reuse, never-raise contract). execute_run WIRING: a pre-run measure guard REFUSES a user_file run on mismatch (stage scenario_measure_guard, nothing spawned); post-run best-effort attach records the source into the trail and additively STAMPS scenario_source_provenance onto both RUN_MODEL artifacts for a user-file run ONLY (a model run stays bit-identical). /save-scenarios now SELECTS the saved file (scenario_source=user_file + intent derived from the file basis) so it runs end-to-end with zero .py editing (ES-2's 19 tests still GREEN). Purely additive - governed headline (TVOG/aggregation) untouched; RE-BASELINING the headline onto user scenarios remains the owner-gated follow-on. spec docs/ECONOMIC_SCENARIO_FILE_FORMAT.md gains §7 (as-implemented engine consumption + monthly rule) and the ES-3 row flips DONE. 29 new tests (tests/test_es3_scenario_source.py, unittest) GREEN; regression GREEN on the runnable numpy-only subset - ES-2 (19), CF-2 (16), GD-4 (15), GUI-1 (9), GUI-5 (8), page-scripts (2); the 8 task7 EndToEndSmoke/ui_app-sha-baseline failures are PRE-EXISTING (identical on pristine main via stash A/B - scipy + pytest unavailable in this network-restricted sandbox, ui_app cutover owner-gated). NEXT: track 4.0f COMPLETE -> priority reverts to general backlog §4.1 (next OPEN: #3 CBIRC 3.0% discount-cap remediation) | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | ES-2 (owner directive 2026-07-08: user economic-scenario files, track 4.0f): GUI UPLOAD page - NEW par_model_v2/viewer/igui_scenarios.py (stdlib-only at import per the GUI-layer contract; ES-1 loader + numpy imported lazily, regression-tested) + /scenarios console page (file pair upload via FileReader, zero .py editing) wired into run_gui.py with nav link; VALIDATE routes the posted texts byte-exact through the REAL ES-1 loader (temp-dir pair, binary writes so the manifest sha256 sees the user's exact bytes) and surfaces every violation as a row/column error table (capped list, exact total preserved); clean pairs get the spec §4.6 summary card + percentile FAN PREVIEW (p5/p25/p50/p75/p95 by projection year for all 12 tenors, EQ_RETURN and the cumulative equity index, inline SVG, series picker) + the C-ROSS <2000 advisory; SAVE persists the pair byte-exact under run_output/user_scenarios/<digest12>/ (digest-keyed like CF-2/GD-4: identical uploads share one copy, a different set never overwrites; persisted-write digest re-verified fail-loud) plus a preview cache, and merges a user_scenarios provenance block (schema, digest, basis, n_scenarios, currency, source, UNSIGNED banner, warnings) into model_inputs.json with GATE INTEGRATION: any recorded run_gate is POPPED (inputs changed -> Task-6 gate must re-clear, binding the block into the reproducibility digest); /scenario-status re-hashes the stored CSV on EVERY read and reports tampered/missing files STALE instead of silently trusting the block; 19 new tests (tests/test_es2_scenarios_gui.py incl. live HTTP round-trips), nav + node script-syntax guards extended, 292 GREEN across ES-1/ES-2/GUI1-5/CF/GD/PC/IGUI-task/agent-lock suites; 8 pre-existing owner-gated ui_app sha-baseline failures unchanged (verified via stash A/B); engine consumption (scenario_source selector, measure guard, run governance trail) is ES-3 - next queued item | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | ES-1 (owner directive 2026-07-08: user economic-scenario files, track 4.0f): validating loader `par_model_v2/stochastic/user_scenarios.py` for schema esg-user-scenarios-1.0 - FAIL-LOUD `load_user_scenario_set()` enforcing every spec §4 rule with row/column reported (exact 15-column header; scenario contiguity 1..N / years complete 1..100 / sorted / no duplicates via positional sequence check; strict finite-numeric cells - blanks/NaN/inf rejected; plausibility bounds rates [-0.05,0.30] EQ_RETURN [-0.99,3.00] inclusive boundaries regression-tested; manifest schema/basis/rate+equity convention/n_scenarios>=100/projection_years==100 checks; csv_sha256 byte-for-byte integrity gate); errors surfaced as structured list (where/row/column/message, report capped at 50 with exact total count preserved) via UserScenarioValidationError + non-raising collect_validation_errors() for the ES-2 GUI; validated UserScenarioSet carries numpy rate cube (N x 100 x 12 tenors) + equity matrix, manifest, digest, UNSIGNED banner and the spec §4.6 summary card (p5/p50/p95 of 10Y rate + EQ_RETURN at years 1/10/50/100, text echo included); C-ROSS <2000-scenario advisory WARNING attached per spec §5; committed templates regression-locked to the loader constants; 41 new tests (tests/test_es1_user_scenarios.py), 244 GREEN across ES-1/ESG/esg-adapter/user-inputs/CF-1/agent-lock/script-syntax suites; purely additive - no governed headline figure touched; engine/GUI integration deferred to ES-2/ES-3 | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | ES directive registered (owner, interactive): user economic-scenario file track 4.0f (ES-1..ES-3) prioritised above the general backlog; format spec `docs/ECONOMIC_SCENARIO_FILE_FORMAT.md` (esg-user-scenarios-1.0: scenario,year + 12-tenor decimal annual spot zero curve 3M..30Y + EQ_RETURN annual total return; 1000 scenarios x 100 years; manifest with basis declaration + csv_sha256) and CSV/manifest templates committed; conventions owner-confirmed interactively; NOTE this registration was YIELDED mid-session on 2026-07-07T19:25Z because a parallel scheduled claude cycle held the lock for 4.1 #2 (HW1F) - its lock was restored (commits 4ed689f/73e21e1) and registration completed after its release; PROTOCOL GAP LOGGED: agent_lock preflight PROCEEDs on same-owner locks, so two claude sessions can clobber each other - candidate hardening item | Directive + spec cycle (no engine code) | (this cycle's AUTO commit) |
| 2026-07-08 | #2 HW1F swaption calibration on live/proxy quote set (MR-001, MR-008): NEW par_model_v2/calibration/hw1f_live_calibration.py - roadmap-#1 three-tier provenance (live_fetch/cached_snapshot/file_fixture) extended to the FULL swaption surface payload (grid+curve+r0+cap, one schema across tiers; SHA-256-sealed snapshots, tamper detection, live-tier validation FAIL-LOUD - bad live data never silently replaced by fixture; live lineage approver UNSIGNED_pending_owner_approval); DictSwaptionSource routes the resolved payload through the SAME LiveSwaptionDataLoader validation the Phase 13 path uses; run_hw1f_live_calibration() executes calibrate() end-to-end per market and emits a PARAMETER CARD (JSON+MD, committed to docs/validation/) with weighted SSE (bps^2), RMSE/max-error bps, convergence incl. a documented Nelder-Mead polish stage when scipy>=1.15 L-BFGS-B ends ABNORMAL (governed calibrator untouched), params-at-bounds DISCLOSURE (a=3.0 upper bound both markets - HW1F limitation, G2++ roadmap #7 is the fix), per-point fit table, gates G-02 PASS/G-12 PASS, lineage, inputs digest (idempotent re-runs), unsigned:true; repository GovernanceStore never touched; evidence builder scripts/build_hw1f_live_calibration_card.py; docs/ESG_CALIBRATION_DATA_INTERFACES.md gains the swaption live-path section; 18 new tests, 146 GREEN across hw1f-live/phase13-hw1f/live-pipeline/GUI-3/calibration/agent-lock suites; purely additive change set - governed headline figures untouched; params UNSIGNED pending owner approval per the item DoD | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | PC-2 (owner directive 2026-07-03: portfolio construction - COMPLETES track 4.0d): THREE NEW MECHANIC FAMILIES + per-product expense/decrement overrides - WL_PAR_2026 whole-life par (RB mechanics under a documented endowment-at-limit convention; joins the run engine's PAR RB line via USER_PRODUCT_LINE_MAP so composed WL books run end-to-end incl. the representative-product TVOG path), TERM_2026 level term assurance (guaranteed SA on death only, no maturity, optional surrender % default 0) and ANNUITY_2026 deferred life annuity (premiums accumulate an asset-share proxy in deferral; from vesting a GUARANTEED monthly annuity of annuity_rate x notional posts to the NEW annuity_guaranteed liability bucket - suffix-classified guaranteed everywhere incl. CF-3 charts + GD-2 drilldown; post-vesting lock-in via lapse_stop_month, death-in-deferral returns the asset share, deferral-only surrender; composer enforces term > deferral); TERM/ANNUITY are documented NON-PAR scope - split_model_points routes them (with GMMB) out of the stochastic PAR book, deterministic CF set covers their cash flows; per-product overrides (acq/renewal expense pcts, fixed monthly expense, mortality/lapse multipliers, OVERRIDE_PARAMS bounds-validated, applied ONLY when explicitly set - absent keys regression-tested bit-identical) thread through _decrements/_premium_expense/_asset_share_proxy in lock-step across all 6 projectors; loader enum + vested-bonus rules + n_par (WL counts PAR) extended, igui_model_points enum/lockstep-literal/aliases updated, default catalogue gains WL_PAR_STD/TERM_STD/ANNUITY_DEF (builder page renders families dynamically); 23 new tests (tests/test_pc2_mechanic_families.py), 194 GREEN across PC/CF/GD/user-inputs/portfolio-generator/nav/script-syntax/agent-lock suites incl. live HTTP roundtrips; 2 pre-existing owner-gated ui_app sha-baseline failures unchanged (verified via stash A/B); NOTE: cycle re-ran after a sandbox VM restart mid-first-attempt - same lock (fresh, claude) reused, work re-applied from scratch | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | CF-2 (owner directive 2026-07-03: cash-flow projection set - COMPLETES track 4.0c): CF set now PERSISTED WITH EVERY EXECUTED RUN - igui_cashflows.attach_cashflow_set_for_run (called best-effort by execute_run step 6b AFTER artifact stamping + GD-4 path attach; never fails the governed run) writes the CF-1 set into run_output/cashflow_set_runs/<digest12>/cashflow_set/ keyed by the CF-1 inputs digest (identical-input runs share one copy via digest-cache hit; later runs never overwrite an earlier run's set - the digest key IS the stale-set guard); attachment block persisted on the GUI-1 job record; load_run_cashflow_set serves the persisted set (schema + digest re-verified, traversal-safe) via /cashflow-data?run=<id>; /cashflows page gains ?run= binding + run-provenance banner; run registry entries surface cashflow_set availability + digest, history table gains a per-run CFs button, compare notes same/differing/missing CF sets; 16 new tests (112 GREEN across CF1-3/GD1-4/GUI4-5/nav/PC/agent-lock/script-syntax suites incl. live HTTP round-trip fresh+cached); live e2e: real smoke execute_run attached the set fresh then digest-cache hit on re-run, GD-4 path attach unaffected; task7 suite: scipy installed this cycle so live-run tests PASS, only the 3 owner-gated ui_app sha-baseline failures remain (pre-existing on main, verified via stash A/B) | DONE | (this cycle's AUTO commit) |
| 2026-07-08 | GD-4 (owner directive 2026-07-07: stepwise detail in GUI - COMPLETES track 4.0e): scenario-path detail now PERSISTED WITH EVERY EXECUTED RUN - igui_path_detail.attach_path_detail_for_run (called best-effort by execute_run step 6 AFTER artifact stamping; never fails the governed run) writes the GD-1 set into run_output/path_detail_runs/<digest12>/path_detail/ keyed by the GD-1 inputs digest, so identical-input runs share one copy (digest-cache hit) and later runs never overwrite an earlier run's set; attachment block persisted on the GUI-1 job record; load_run_path_detail serves the persisted set (schema + digest guards, traversal-safe) via /path-data?run=<id>; /paths page gains ?run= binding + run-provenance banner; run registry entries surface path_detail availability + digest, history table gains a per-run Paths button, compare notes same/differing/missing path sets; 15 new tests (154 GREEN across GD1-4/GUI1-5/CF/PC/nav/script-syntax/agent-lock suites incl. live HTTP round-trip fresh+cached); 8 pre-existing task7 sha-baseline/live-run failures on main unchanged (verified via stash A/B; full run_model smoke not runnable this cycle - scipy wheel download stalled in the sandbox, GUI surface e2e covered over live HTTP instead) | DONE | (this cycle's AUTO commit) |
| 2026-07-07 | GD-3 (owner directive 2026-07-07: stepwise detail in GUI): run_result_decomposition.py engine - READ-ONLY decomposition of the executed run's RUN_MODEL_AGGREGATION_REPORT.json into a 14-step SCR build-up waterfall (7 standalone driver SCRs -> sum -> var-covar diversification credit -> copula tail-dependence adjustment -> nested interaction residual -> headline nested SCR; identities asserted fail-loud, artifact-name lock-step regression vs run_model.py), per-driver share table, copula AIC-candidate evidence, tail-convergence + bootstrap CI carry-through; JSON+4 CSVs, digest-cached over artifact bytes, UNSIGNED note; igui_decomposition.py /decomposition page (floating-bar waterfall SVG with connectors + tooltips, provenance card, tables, convergence chart) + /decomposition-data route + nav link; SCOPE NOTE: TVOG build-up not in the executed-run artifact set (separate owner-gated pipeline, headline untouched); 15 new tests, 47 GREEN across GD/nav/script-syntax suites, 108 passed on wider GUI/CF subset; live e2e fresh+cached verified; 10 pre-existing failures on main unchanged (verified via stash A/B) | DONE | (this cycle's AUTO commit) |
| 2026-07-07 | GD-2 (owner directive 2026-07-07: stepwise detail in GUI): liability_drilldown.py engine - per-model-point AND per-product-class stepwise monthly build-up (in-force/death/surrender counts + all 9 CF-1 buckets + gtd/non-gtd subtotals + cumulative net; reuses CF-1 _PRODUCT_PROJECTORS + resolve_portfolio so class views reconcile EXACTLY with the CF-1 set - regression-tested; JSON+2 CSVs, digest-cached over portfolio+catalogue, UNSIGNED) + igui_drilldown.py /drilldown page (selection picker, lifetime KPIs, yearly SVG build-up chart gtd vs non-gtd, yearly table with per-year monthly drill-down) + /drilldown-data route + nav link; 15 new tests, 60 GREEN across GD/CF/nav/script-syntax suites; live e2e fresh+cached verified; 12 pre-existing failures on main unchanged (verified via stash A/B) | DONE | (this cycle's AUTO commit) |
| 2026-07-07 | GD-1 (owner directive 2026-07-07: stepwise detail in GUI): scenario_path_detail.py engine (P-measure HW1F + correlated GBM on saved seed; per-class return/cumulative fans via CF-1 mechanics: bond carry+duration proxy, equity TR, cash carry; JSON+6 CSVs, digest-cached, UNSIGNED) + igui_path_detail.py /paths page (fan charts p5-p95/p25-p75/median + sample overlays, series picker, provenance card) + /path-data route + nav link; CF-3 liability chart now splits guaranteed vs non-guaranteed benefits (schema cf3-gui-1.1); 13 new tests, 114 GREEN across GD/CF/PC/GUI suites incl. nav + node script-syntax guards; live e2e fresh+cached verified | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | PC-1c (owner report: pages not inter-accessible): shared nav bar now injected CENTRALLY (run_gui._with_nav) on all 11 console pages with active-page highlight; page-embedded duplicates removed; new regression test asserts every page carries exactly one nav with all links; 76 GREEN (4 pre-existing sha-baseline failures unchanged) | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | PC-1b (owner request): GUI navigation bar on the Run Controls landing page + all-console links; /portfolio construction blocks presented as TABS (Asset strategy / Product catalogue / Portfolio composer); node JS guard green; 46 tests GREEN (2 pre-existing sha-baseline failures unchanged) | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | PC-1 portfolio construction (owner directive): asset SAA (type/mix/weights -> derived balance sheet incl. run-engine fields backing_asset_mv/illiquid_share + CF fund mechanics), product catalogue over 3 mechanic families (short/long par etc.), composer + /portfolio GUI page; saves loader-revalidated, gate reset on save; CF outputs split per catalogue product; 17 new tests, 130 GREEN across suites; LIVE e2e construct->run->cashflows verified (2 pre-existing sha-baseline failures unchanged) | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | CF-3 (owner request): /cashflows GUI page - inline-SVG charts (liability CF components; stacked asset balances), yearly tables per class with monthly drill-down per year, digest-cached /cashflow-data endpoint (refreshes the six CSVs), UNSIGNED banner, run-off/shortfall callouts; page added to node JS-syntax guard; 6 new tests, 85 GREEN across CF+GUI suites | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | CF-1c (owner correction: asset balances were level): asset fund now COUPLED to liability net CF - income reinvested, benefits funded by sales, monthly constant-mix rebalancing, zero-floor + shortfall reporting, book_runoff_month marker; verified grow-then-run-off (peak y14 524M, -146.6M y20 maturities); 22/22 tests GREEN incl. accounting identity | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | CF-1b (owner request): wide-format CF artifacts - rows = time only, classes horizontal (<class>__<measure> headers; balances use plain class labels); JSON preview follows; 2 new pivot tests + updated artifact tests, 18/18 GREEN | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | CF-1 cash-flow projection set (owner directive): par_model_v2/projection/cashflow_projection_set.py - liability by product class x 9 buckets (gtd/non-gtd x death/maturity/surrender + cash dividend; CD product = 6 populated buckets), asset CF + balances by class, monthly+yearly to 100y, JSON+6 CSV artifacts with inputs digest; premium/expense/decrement conventions regression-tested vs legacy engine; 16 new tests GREEN, 98 legacy projection tests GREEN | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | HOTFIX GUI-5 (owner report: button did nothing): collapsed backslash escape put raw newlines inside JS string literals -> whole page script failed to parse -> no click handler; 21 escapes fixed; NEW GUARD tests/test_igui_page_scripts_syntax.py (node --check over every inline script of all 9 GUI pages); live e2e re-verified (seed 45 smoke run succeeded, headline rendered) | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | GUI-5 (owner request): Save & RUN button on Run Controls page; /save-run orchestration (save → autofill missing domains with governed defaults → gate → GUI-1 async job); gate never bypassed (engine re-verifies); 8 new tests GREEN; live e2e: one click → smoke run succeeded, headline rendered, /my-results refreshed; pre-existing sha-baseline failures unchanged | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | GUI-4 run history & compare: igui_run_history.py + /history /runs /runs/<id> /compare-runs; registry from persisted GUI-1 job records (id, timestamps, inputs digest, seed, headline); durable run-plan enrichment (survives artifact overwrite); side-by-side metadata + headline-delta compare with smoke/kind/same-digest notes; 14 new tests GREEN (53 across GUI-1..4); GUI TRACK COMPLETE | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | GUI-3 calibration console: igui_calibration.py + /calibration /calibration-catalogue /market-data-status /run-calibration; HW1F + GBM pipelines async via JobManager; per-market parameter card + fit diagnostics (RMSE/SSE-proxy, convergence, gates G-02/G-12/G-03); market-data provenance panel (roadmap #1); UNSIGNED surfaced everywhere; repo governance store untouched (byte-tested); new tests GREEN | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | GUI-2 stress console: igui_stress.py + /stress /stress-catalogue /run-stress /asset-stress; JobManager per-job runner+meta; 13 new tests GREEN (25 with GUI-1 suite); live base-vs-stress e2e verified (conf 99.0 → SCR −13.9%); no new regressions vs main baseline | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | GUI-1 async run console: igui_job_manager.py + /execute-async + /jobs endpoints + polling run page; 9 new tests GREEN; live async end-to-end smoke succeeded (real engine headline, /my-results refreshed); 7 pre-existing ui_app sha-baseline failures on main documented (owner-gated Phase 38) | DONE | (this cycle's AUTO commit) |
| 2026-07-03 | Owner directive registered: GUI run-console track (GUI-1..GUI-4) prioritised above general backlog; architecture = local web server | Directive cycle (no code) | (this commit) |
| 2026-07-03 | Roadmap created; sync tooling (`scripts/cowork_sync_push.sh`) added; scheduled task registered | Setup cycle | — |
| 2026-07-03 | #1 Live market-data pipeline: `live_market_data_pipeline.py` (CNYYieldCurveLoader, CSI300IndexLoader, SnapshotCache), 2 fixtures, 12 tests GREEN; live tier UNSIGNED pending owner source approval | DONE | (this cycle's AUTO commit) |

## 6. Standing Rules

- Never force-push `main`; never bypass the agent lock.
- Governed headline figures (TVOG, aggregation reports) change only with an explicit item covering them.
- Secrets (GitHub PAT at `.git/gh_token`) are never committed, logged, or echoed.
- If two consecutive cycles yield (lock contention with Codex), report to Model Owner instead of retrying harder.
