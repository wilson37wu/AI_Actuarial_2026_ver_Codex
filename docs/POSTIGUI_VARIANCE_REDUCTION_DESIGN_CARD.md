# Inner-Path Variance Reduction (TVOG) - Design Card (Post-Phase-IGUI Task 3)

**MR-VR-1 - Inner-path antithetic / CRN variance reduction for the TVOG estimator.** Pure governance pre-registration; NO parameter change; implementation DEFERRED. EFFICIENCY only.

## What it would do (next cycle, gated)

Run a DISCLOSED efficiency study on the inner-path / nested-stochastic TVOG estimator: compare crude i.i.d. Monte Carlo against antithetic pairing, common-random-numbers (CRN) across the guarantee on/off legs, and an optional Sobol-RQMC inner grid - all on the SAME governed outer states. Report unbiasedness (mean over >= 200 replicate seeds within 0.5% of crude), work-normalised variance-reduction ratios with CIs, and the inner-path count n* for a target SE. The governed production estimator and headline SCR (39,975.7) stay BIT-IDENTICAL - this is measurement, not adoption.

## Why now

MR-CAL-1 calibration diagnostics completed at Task 2 (margin residual 8.15% of the nested gap; copula-FORM dominates). With model FORM frozen by the Phase 30 binding stop-rule, the next worthwhile gain is NUMERICAL: the inner-path TVOG estimator is the most compute-intensive part of a run and its variance sets the inner-path budget. Antithetic and CRN are standard, bias-free levers; the study must MEASURE inner-path efficacy (antithetic is expected-ineffective at 99.5%).

## Sequencing

1. **MR-CAL-1** credentialled-data calibration diagnostics (COMPLETE, Task 2).
2. **MR-VR-1** inner-path antithetic/CRN variance reduction for TVOG (selected now).
3. **MR-LONGEV-1** longevity 5th driver (deferred; needs owner sign-off).

*Generated by scripts/build_postigui_task3_design_note.py.*
